# Curator missing-axis collection status

The three requested axes remain unreleased. Their source records are not present locally,
and the required Fetcher role cannot be launched from this union-repository host: its
launcher correctly refuses the preparation host because a standalone role session must run
on a separate machine without the union repository. Direct network retrieval from this host
would not be a Fetcher-scope measurement and would not repair the failed runtime boundary.
No claim from this stage is admissible until that boundary holds.

The retained negative candidate file contains 32,225 reviewed proteins that do not match the
positive union, but no retained source gives positive experimental evidence that any is a
non-effector. They remain unlabelled; verified-negative count remains zero. Required external
sources for a usable verified-negative set are experimental localisation, characterised
housekeeping-function, or saprotroph-secretome evidence records, each with provenance and
evidence code.

All 22 subject model cards remain undated. Their developer documentation must supply a stated
cutoff, or an explicitly marked inference, before publication-year records can be stratified.
No dated card permits a pre/post-cutoff calculation. RNA-seq, repeat annotation and assembly
quality likewise need source-backed records per each of 66 species; none are retained. Thus
0/66 currently reach three documented channels and no corrected-panel count for that subset is
estimable.

This is a task blocked by the failed role-runtime deployment, not an assertion that the public
sources do not exist. The runtime condition is already recorded as Block A with external holder
the host sysadmin. Clause 1 prevents census release and Gate 1 convening.
