# Clause 14 block audit

Date: 2026-09-15

Seven entries with solely project-internal holders were moved from
`handoff/gate0/blocked.md` to the task register or their existing task rows.  Their
original events are retained here so the audit does not erase history.

| Former blocked entry | Internal holder | Destination as work |
|---|---|---|
| T1.1 | Curator | `T1.1` (`partial`) |
| T1.3 | Curator; Instrument | `T1.3` (`partial`) |
| T1.3 sequence-30 | Curator | `T1.3`; MMseqs2 repair in this stage |
| T1.6 | Fetcher | `T1.6` (`partial`) |
| Block B production scoring | Referee | `T4.3` in `tasks/register.csv` |
| Block C production floor statistics | Floor; Referee | `T2.4` and `T2.5` in `tasks/register.csv` |
| Curator boundary probe | Curator / access renderer | `TR.2` (`partial`) |

This is a record-quality finding, not an external project impediment: seven work
items had been mislabeled as blocks.  The still-open blocked entries retain an
external holder.
